payments-database:
  image: postgres:alpine
  restart: always
  hostname: payments-database
  ports:
    - "5432:5432"
  volumes:
    - /var/lib/postgresql/data

payments:
  build: .
  restart: always
  hostname: payments
  ports:
    - "5000:5000"
  links:
    - payments-database
